# D-Logic AI プロジェクトチェッカー

このエージェントは `/agent` コマンドで呼び出し、プロジェクトの実装が指示通りになっているかをチェックします。

## チェック項目

### 1. I-Logic自然言語処理の実装状況
- [ ] Supabase専用アーカイブハンドラーが実装されている
- [ ] フロントエンドAPI依存が削除されている
- [ ] 自然言語でのレース検索が可能（例：「新潟2R分析して」）
- [ ] 日付推論が動作する（今日、明日、先週、今週末）
- [ ] レース名検索が動作する（札幌記念など）
- [ ] 複数候補時に選択肢が表示される
- [ ] エラー時に適切なメッセージが表示される

### 2. 名称変更の確認
- [ ] UI表示で「レースアナリシス」→「I-Logic」に変更済み
- [ ] チャット画面のタイトルが「I-Logic」
- [ ] AIヘッダーが「👑 I-Logic AI」
- [ ] バックエンドのレスポンスで「I-Logic分析」と表示
- [ ] APIエンドポイント名は変更していない（race_analysis_v2のまま）

### 3. UIデザインの統一
- [ ] I-Logicチャットの吹き出しが黒背景（D-Logicと同じ）
- [ ] MyLogicチャットがD-Logicと同じデザイン（黄色ユーザー、黒背景AI）

### 4. 環境変数の設定
- [ ] RenderにSUPABASE_URLが設定されている
- [ ] RenderにSUPABASE_ANON_KEYが設定されている
- [ ] バックエンドの.envファイルに必要な設定がある

### 5. データベースの状態
- [ ] Supabaseのarchive_racesテーブルにデータが存在する
- [ ] 2025-08-16と2025-08-17のレースデータが入っている
- [ ] 新潟、中京、札幌のデータが含まれている

### 6. エラーハンドリング
- [ ] Supabase接続エラー時に明確なメッセージ
- [ ] レースが見つからない時の適切な案内
- [ ] ログに詳細なエラー情報が記録される

## 実行方法

```bash
# このチェッカーを実行するには、チャットで以下を入力：
/agent プロジェクトチェック
```

## チェック結果の報告形式

```
✅ 完了項目: X/Y
❌ 未完了項目: Z個

詳細：
1. I-Logic自然言語処理
   ✅ Supabase専用ハンドラー実装
   ❌ エラー: Supabase環境変数未設定
   
2. 名称変更
   ✅ すべて完了
   
...
```

## 修正が必要な項目への対応

各未完了項目に対して：
1. 問題の詳細説明
2. 修正方法の提案
3. 必要なコードやコマンド
4. 確認方法

## エージェント呼び出しコード

```python
# /agent コマンドハンドラー（概念的な実装）
def handle_agent_command(command):
    if command == "プロジェクトチェック":
        return ProjectChecker().run_all_checks()
    elif command == "I-Logic検証":
        return ProjectChecker().check_ilogic_implementation()
    # ... 他のコマンド
```